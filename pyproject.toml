[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "witness_stand"
version = "1.0.0"
description = "OpenEnv adversarial witness RL environment"
requires-python = ">=3.11"
